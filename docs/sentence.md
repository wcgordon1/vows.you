Must enforce: no preview auth/billing; no vow text in logs; webhook-only unlock; no HTML rendering of vows.Must enforce: no preview auth/billing; no vow text in logs; webhook-only unlock; no HTML rendering of vows.

http://localhost:3000/